import 'package:flutter/material.dart';

class LifeGame extends StatelessWidget {
  const LifeGame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
